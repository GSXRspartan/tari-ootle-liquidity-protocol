{
  "name": "@tari-ootle/wallet-adapter",
  "version": "0.1.0",
  "description": "Wallet adapter interface and implementations for Tari Ootle",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No lint configured'",
    "test": "npm run build && node --test test/marketplace.test.cjs test/amm.test.cjs"
  },
"dependencies": {
    "@tari-ootle/protocol-client": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "license": "MIT",
  "//": "@chironbuilder/ootle-sdk was removed: its published releases declare relative file: deps (file:./vendor/ootle-patched, file:./vendor/ootle-wasm-patched) that npm resolves inside the tarball but pnpm resolves against the depending project (ERR_PNPM_LINKED_PKG_DIR_NOT_FOUND). No source in this workspace imports the SDK; when a browser provider needs it, depend on @tari-project/ootle-* upstream packages directly (docs/TARI_BROWSER_ATOMIC_SWAP_PROVIDER_GAP.md)."
}

