{
  "name": "tari-ootle-liquidity-protocol",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Non-custodial liquidity protocol for Tari Ootle",
  "packageManager": "pnpm@9.15.9",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0 <10.0.0"
  },
  "//engines": "pnpm is pinned to major 9 because CI uses pnpm/action-setup version 9 and pnpm-lock.yaml is lockfileVersion 9.0. A pnpm 10/11 lockfile cannot be consumed by a pnpm 9 install, so the major is constrained rather than left open."
}
