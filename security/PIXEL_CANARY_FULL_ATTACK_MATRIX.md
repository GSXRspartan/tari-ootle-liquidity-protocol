# Pixel Canary — full attack matrix

Independent adversarial pass over the whole repository, by an approach explicitly
*not* reusing the conclusions of the earlier subsystem matrices
(`CROSS_LAYER_ATTACK_MATRIX.md`, `MULTIHOP_ATTACK_MATRIX.md`,
`FRONTEND_ATTACK_MATRIX.md`, `LP_HISTORICAL_ATTACK_MATRIX.md`,
`DEPLOYMENT_SECURITY_MATRIX.md`, `MARKET_DATA_RESIDUAL_RISKS.md`). Those were read
as hypotheses; rows below that reuse them are marked with the evidence that was
re-executed here rather than trusted.

## Scope and method

- Every row was either (a) executed against the code by a test written in this
  pass, (b) re-executed from an existing suite, or (c) attempted and stopped by an
  external or tooling blocker that is named in the row.
- Evidence pointers are files in this repository, never narrative claims.
- `UNKNOWN` is not an allowed result. Where a risk could not be closed it is
  classified `EXTERNAL_RISK`, `BLOCKED_EXTERNAL`, or `BLOCKED_TOOLING`.

## Result legend

| Result | Meaning |
| --- | --- |
| `PASS` | The attack was attempted (here or by a re-run suite) and the invariant held |
| `FIXED` | A real defect was found in this pass, root-caused, fixed, and given a permanent regression |
| `N/A_BY_CONSTRUCTION` | The architecture makes the attack unreachable |
| `EXTERNAL_RISK` | Invariant holds locally; the residual exposure sits in an external system |
| `BLOCKED_EXTERNAL` | Closure requires infrastructure this environment does not have |
| `BLOCKED_TOOLING` | Closure requires a platform/tool this environment cannot run |

---

## A. Outage, discovery, and honest failure states

| ID | Subsystem | Attack | Prerequisite | Exploit sequence | Invariant | Evidence | Result | Severity | Fix | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-01 | web / pool discovery | Hostile or hung indexer makes the app spin forever | Indexer host reachable at TCP level but not answering | Load app → `fetch` never settles → `refreshPools` never resolves → `loading` stays true → no outage state, user cannot distinguish slow/broken/empty | Every discovery call terminates within a deadline | `apps/web/test/outage.test.cjs` → "a host that accepts the request and never answers still resolves", "pool discovery reports unavailable instead of hanging" | FIXED | MEDIUM | `services/net.ts` `postJson` with `AbortController` deadline (12 s); discovery sources now always return | Deadline is a policy constant; a slow-but-alive indexer inside 12 s still delays first paint |
| PC-02 | web / discovery | Memory DoS via oversized discovery body | Hostile `indexerUrls[0]` | Return a 300 MB JSON document → `response.json()` buffers it whole → tab memory exhaustion | A discovery response is never buffered past a cap | `outage.test.cjs` → "declared oversized body is refused before it is buffered", "undeclared oversized body is refused after the cap" | FIXED | MEDIUM | `content-length` pre-check plus hard read cap (2 MB) in `net.ts`; body is never read when the declared size is refused | A chunked hostile response is still read up to the cap before rejection |
| PC-03 | web / NFT discovery | Failure rendered as a legitimately empty marketplace | Indexer returns HTTP 5xx / malformed body | `IndexerMarketplaceSource.query` returns `[]` on failure → page shows "No collections" → outage indistinguishable from an empty market | "We could not read" must never be rendered as "there is nothing" | `outage.test.cjs` → "a failed marketplace query is distinguishable from zero collections", "records that are all unreadable are reported as unreadable" | FIXED | MEDIUM | Failure reason recorded on the source and surfaced by `NftMarketplacePage`; unparseable-record case gets its own reason | Wording only; no in-app retry/backoff is offered |
| PC-04 | web / config | Malformed `VITE_INDEXER_URL` crashes the app at render | Deployment-time misconfiguration | `new IndexerPoolDiscovery('garbage')` → `new URL()` throws inside `useMemo` during render → blank page with no error | A bad configuration is an error state, not a crash | `outage.test.cjs` → "a malformed configured endpoint fails closed instead of throwing" | FIXED | MEDIUM | Constructor no longer throws; source reports `indexer:invalid` and fails closed | `config.blocking` does not yet pre-validate URL shape; the failure appears at discovery time |
| PC-05 | web / app state | Throwing discovery source wedges the loading state | Any source that throws | `refreshPools` has no `try/catch` → rejected promise → `loading` never cleared → permanent skeleton | The app always leaves the loading state | `apps/web/src/state/AppContext.tsx` (`refreshPools`); covered by the shell render tests and the bounded-source tests | FIXED | LOW | `try/catch` in `refreshPools` producing an `unavailableReason` | Same coverage as PC-01; a throw inside `presentHealth` is not trapped |
| PC-06 | web / activity | Persisted `CONFIRMED` presented as an authoritative fact | Any script on the origin, or a synced/edited `localStorage` | Write `{state:'CONFIRMED'}` → Activity shows a green CONFIRMED badge with "An authoritative lookup proved the transaction committed" although no lookup happened; the Reconcile button was disabled for `CONFIRMED`, so the claim could not be checked | Local storage is a claim; only a chain reread is proof | `lib/storage.ts` `trustLevel` (existed, never called); regression added in `e2e/security.spec.ts` → "a tampered confirmation is shown as a claim, not as settled" now asserts the claim wording and the enabled verification action | FIXED | MEDIUM | `ActivityPage` renders `trustLevel(record)` → "Unverified claim" badge + explanation notice; verification button enabled for claimed records | A tampered record can still be *absent* (deleted), which costs the user a pointer, not a fact |


---

## B. Deployment, CI, and documentation truthfulness

| ID | Subsystem | Attack | Prerequisite | Exploit sequence | Invariant | Evidence | Result | Severity | Fix | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-07 | CI / deploy | A second host publishes the app with **no** security headers | `pages.yml` runs on push to `main` | Merge → GitHub Pages deploys → Pages ignores `dist/_headers` → no CSP, no `frame-ancestors`, no nosniff/COOP/CORP/Referrer-Policy → the transaction-signing UI is frameable by any origin and the reviewed policy is silently absent, while CI still "passes" a build-time `_headers` existence check | A deploy path must never silently downgrade the security policy | `.github/workflows/pages.yml`; `docs/GITHUB_PAGES.md` (which asserted "CSP header enforced" on this host) | FIXED | HIGH | `pages.yml` no longer runs on push; it requires manual dispatch plus `acknowledge_no_security_headers`, otherwise it fails with an explicit error and writes the downgrade to the step summary | The capability still exists if someone deliberately acknowledges it |
| PC-08 | docs / deployment | Docs claim a Cloudflare deploy workflow that does not exist | Reader trusts `TESTNET_HOSTING.md` | Doc says "the deploy workflow authenticates to Cloudflare with an API token" → `git ls-files .github/workflows` shows only `node-tests`, `pages`, `security-engine-tests` → an operator believes R-1's delivery path exists when it does not | Documentation may not describe machinery that is absent | `docs/TESTNET_HOSTING.md` (before/after) | FIXED | MEDIUM | Withdrawn explicitly; state recorded as `OPEN / BLOCKED_EXTERNAL` with an exact 5-step closure procedure | Closure still requires credentials this environment does not have |
| PC-09 | deployment | Security headers never observed on a real HTTPS origin | No Cloudflare credentials in this environment | `_headers` authored → locally served → browser-tested → deployed (never) → R-1 cannot close | R-1 closes only on a live observation | `docs/TESTNET_HOSTING.md` § Exact minimal closure procedure | BLOCKED_EXTERNAL | HIGH (open) | — | Headers could differ on the real host; R-1 stays OPEN |
| PC-10 | CI | Windows cannot build the engine suite, so it is silently skipped | Local Windows runner | `wasmer-compiler-cranelift` emits a `compile_error` on Windows → no local engine evidence | The engine suite must run somewhere, never be skipped | `.github/workflows/security-engine-tests.yml` → `cargo test --manifest-path audit_engine_tests/Cargo.toml` on `ubuntu-latest`, job fails if it cannot compile | BLOCKED_TOOLING | INFO | — | Local Windows execution is not available; evidence is CI-only |
| PC-11 | CI | Supply-chain drift reaches a deployment | Maintainer bumps a manifest without review | `pnpm install` re-resolves a malicious transitive package | Frozen lockfile, pinned pnpm major | `pnpm install --frozen-lockfile` in `node-tests.yml`, `pages.yml`; pnpm pinned `9.15.9`; `pnpm audit --prod` → "No known vulnerabilities found" (this pass) | PASS | INFO | — | `pnpm audit` reached the registry from this environment for prod only; full-tree audit is CI-side |
| PC-12 | CI / regression | A suite is silently skipped | Runner can only run some projects | e.g. browser tests drop to Chromium-only and nobody notices | Every suite that is claimed green must actually run | `node-tests.yml` runs typecheck+tests for all three packages, production build, `_headers` assertions, and `test:e2e:all` (Chromium **and** Firefox) with failure artifacts uploaded | PASS | INFO | — | Firefox instability is local (see PC-24) |

---

## C. Wallet / provider trust, shown == signed, secrets

| ID | Subsystem | Attack | Prerequisite | Exploit sequence | Invariant | Evidence | Result | Severity | Fix | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-13 | wallet | Hostile `window.tari` is trusted on shape alone | Page script control or a rogue extension | Replace the provider after review → the app signs under a different account/network than shown | Object identity + capability handshake + live re-derivation, checked at authorization time | `lib/executionIdentity.ts` (`captureIdentity`/`verifyIdentity`), `services/walletService.ts` `liveIdentity` (re-resolves the provider **from `window`**, not from cache); `e2e/security.spec.ts` provider-spoof and capability-handshake tests | PASS | HIGH | — | A provider that lies *consistently* (right fingerprint, wrong intent) is only caught by the wallet's own review |
| PC-14 | wallet | Network/account changes after review, before sign | User switches account/network during approval | Review for account A, sign under account B | Live identity must equal reviewed identity on every submission | `executeSwap` Gate 1 → `IdentityChangedError`; `AppContext` re-issues the identity on account/network change; hostile tests in `hostile-frontend.test.cjs` (TOCTOU group) | PASS | HIGH | — | None found in the tested model |
| PC-15 | wallet | Review is mutated between approval and signing | Another script running on the origin | Mutate `review.walletRequest` after approval → shown != signed | The signed request must be frozen | `executeSwap` Gate 3 rejects an unfrozen review or an unfrozen `walletRequest`; `signAndSubmit` receives `review.walletRequest`, it is never re-derived from the preview | PASS | HIGH | — | Freezing is shallow-shallow; nested objects deep-frozen by `freezeReview` |
| PC-16 | wallet | Reviewable field silently differs from builder intent | Any intent | Drift in `min_output`, resource address, expiry, NFT identity | Review must be a differential match to the intent | `lib/review.ts` `diffReviewForIntent` per intent kind; `executeSwap` Gate 2 → `ReviewMismatchError`; hostile-frontend "request integrity" group | PASS | HIGH | — | Requires the intent kind's diff to be complete; new intent kinds must add a diff |
| PC-17 | secrets | Preimage/secret leaks into UI, logs, storage, URLs, history | Any logging/storage path | Secret appears in console, `localStorage`, operation record, error text, URL | A secret never reaches any output channel | `e2e/security.spec.ts` → "no secret is ever written to storage", "the app logs nothing to the console"; `hostile-frontend.test.cjs` secrets group; `storage.ts` `FORBIDDEN_PERSISTED_FIELDS` reject-on-sight; `route.test.cjs` → "the preimage is never present on a view the UI can render" | PASS | CRITICAL | — | Third-party scripts injected by the wallet frame are outside this app's control |
| PC-18 | amounts | Typed amount differs from signed amount | Hostile paste, Unicode digits, scientific notation | `parseFloat('0x10')`, `1e3`, `١٢٣`, `1.0000000` with 6 decimals | Exact integer parsing only; excess precision is an error, never a truncation | `lib/amountInput.ts` `HOSTILE_AMOUNT_CASES` (30 cases); boundary funnel `asRawExecutionAmount`; hostile-frontend amounts group | PASS | CRITICAL | — | None found; `MAX_DECIMALS` cap is 36 |
| PC-19 | boundary | Market-data value becomes an execution input | Hostile indexer response | `resolveSwap({ rawInputAmount: marketData.price })` | Display values are a different type than execution inputs | `lib/tradeBoundary.ts` `DisplayOnly` brand (runtime symbol, not type-only) + runtime guard; `boundary` tests in `security.test.cjs` | PASS | CRITICAL | — | Values arriving as `any` are caught by the runtime guard |

---

## D. Protocol subsystems re-attacked

Rows in this section reuse the existing hostile suites as evidence **only where
this pass re-executed them**. Engine-scope rows that were re-run in this session
are marked `PASS`; those whose evidence could not be re-executed are marked with
the CI job that owns them (`PC-10`) rather than being claimed green locally.

| ID | Subsystem | Attack | Prerequisite | Exploit sequence | Invariant | Evidence | Result | Severity | Fix | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-20 | AMM | Rounding extraction / repeated dust theft, denominator error, fee bypass, share inflation, first-depositor and donation/inflation attacks, floor-to-zero mint or redeem | Devotes an independent model rather than reusing implementation math | Compare swap/LP mint/LP redeem against `crates/pool_ref_model` reference and the fuzz/gauntlet suites | LPs cannot lose value to rounding; shares are backed | `crates/pool_ref_model/tests/state_fuzzer.rs`, `pool_math` property tests, `audit_engine_tests/tests/pool_{adversarial,economics,engine}.rs`, protocol-client AMM hostile suites — protocol-client suite **re-executed this session (185/185 PASS)** | PASS | CRITICAL | — | On-chain execution evidence is CI-only on Linux (`PC-10`) |
| PC-21 | LP accounting | Reserve/fee desync, zero-liquidity pool, zero supply, hostile/duplicate resources, fake canonical TARI, malformed buckets, replay | Same as above | Force impossible pool state through the resolver | The resolver refuses states the pool can never legitimately be in | protocol-client hostile suites (re-executed: 185/185) + engine suite in CI | PASS | CRITICAL | — | Same as PC-20 |
| PC-22 | NFT marketplace | Stale listing/offer/bid, double-accept, self-purchase, partial collection-bid fill, quantity and escrow accounting, fake collection / same-name-different-resource, wrong quote resource, expiry, replay, restart | Discovery data is attacker-controlled | Forge discovery payload, attempt to execute against it | Discovery is never execution authority; the resolver rereads the component | `marketplace.ts` resolvers + protocol-client marketplace hostile tests (in the 185) + engine suite `marketplace_{engine,item_offer_engine,collection_bid_engine}.rs` in CI | PASS | CRITICAL | — | Engine evidence CI-only |
| PC-23 | Cross-layer / L1 authority | Provider asserts an amount that was never observed; remembered intent or cached quote treated as authoritative | Hostile provider | Submit a route whose L1 leg claims a different amount | `amountAuthoritative === false` must fail closed everywhere | `security/MINOTARI_AUTHORITY_MODEL.md`; coordinator `isRealSubmitEnabled` gates at three call sites (`coordinator.ts:241,390,504`); cross-layer hostile suite | PASS | CRITICAL | — | Base-node readback fundamentally cannot reveal a plaintext amount; the system must keep failing closed |
| PC-24 | Multi-hop | Hop 2 starts from optimism or a cached/stale proof; proof replay across sessions/accounts/networks; completed hop 1 + failed hop 2 loses user TARI | Hostile/absent coordinator state | Pause a route after hop 1 | The user owns the intermediate asset and the route is paused, not "failed" | `multihop/proof.ts` branding, `multihop_fuzz.test.cjs`, hostile route tests (in the web suite: partial-completion, requote-floor, unknown→recovery cases re-executed this session) | PASS | CRITICAL | — | Recovery still depends on the user reconciling by durable id |
| PC-25 | Market data | Trade-id collision, late/out-of-order arrival, candle rebuild, SSE malformed input, backfill/live overlap, duplicate epochs, memory growth | Hostile indexer stream | Feed the aggregator bad observations | Aggregation is exact-rational, deduplicated, and bounded | `marketdata_fuzz.test.cjs` + market-data hostile suites (in the 185, re-executed) | PASS | HIGH | — | Unbounded *subscription* lifetime is bounded by component teardown |
| PC-26 | Routing | Fake "best" route, route cycles, duplicate assets, provider spread inconsistency, amount mismatch, unsafe fallback | Hostile provider quotes | Return an inconsistent quote | A route is refused unless every hop's quote is internally consistent | `route.test.cjs` / hostile route group (re-executed in the web suite) | PASS | HIGH | — | No arbitrary graph routing exists, by design |
| PC-27 | Minotari script | Truncated/extra opcodes, alternate varint encodings, trailing bytes, wrong refund branch, SHA-256 binding failure | Malformed script bytes | Feed the decoder adversarial bytes | The decoder refuses anything that is not canonical; refund branch derives exactly | `crates/protocol_types` tests + `minotari.test.cjs` (17/17, in the re-executed 185) | PASS | HIGH | — | Fuzz depth is bounded by suite time; seeds are persisted |

---

## E. Browser client, persisted state, and UX-as-security

| ID | Subsystem | Attack | Prerequisite | Exploit sequence | Invariant | Evidence | Result | Severity | Fix | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-28 | web / untrusted metadata | Token, NFT, or collection metadata carries script, `javascript:`/`data:` URLs, bidi/zero-width spoofing, oversized strings, malicious SVG | Hostile collection or token metadata | Serve hostile JSON/SVG → render | Nothing renders as markup; no dangerous URL scheme ever navigates | `lib/sanitize.ts` (`safeLabel`, `safeExternalUrl`, `safeImageUrl` — control chars, bidi overrides, credentials, scheme allowlist); `security.test.cjs` xss group; `e2e/security.spec.ts` hostile-metadata browser test (re-executed) | PASS | CRITICAL | — | Images are still fetched from arbitrary https hosts (by design, with `no-referrer`) |
| PC-29 | web / URL | Explorer or media URL injection | Attacker-controlled tx id | Build `javascript:` link from a tx id | An href exists only from a known-good template | `sanitize.ts` `explorerUrl` returns `undefined` when no verified template exists; `security.test.cjs` href/src assertion | PASS | HIGH | — | None |
| PC-30 | web / persisted state | Tampered history JSON: forged `CONFIRMED`, preimage field, prototype pollution, oversized payload, invalid BigInt, version mismatch | Local attacker | Write hostile payload to `ootle.operations.v1` | Live path rebuilds from an allow-list and fails closed | `lib/storage.ts` `loadHistoryPayload` (size cap, list check, per-record allow-list rebuild, forbidden-field rejection) — `history.ts` calls it for **every** read path (`listOperations`, `historyIntegrity`); `hostile-frontend.test.cjs` storage group; `e2e/security.spec.ts` tampered-record test (re-executed) | PASS | HIGH | — | Records above `MAX_RECORDS` (500) are truncated, not error-rejected |
| PC-31 | web / races | Stale async response overwrites current account/network/route; route change mid-flight; double submit | Fast navigation, wallet churn | Old promise resolves after a newer state | Requests are cancelled and identity is re-checked at authorization | `lib/asyncGuard.ts`; cancellation flags in services; e2e provider-change and double-click tests (re-executed) | PASS | HIGH | — | None found |
| PC-32 | web / status rendering | UNKNOWN is rendered as red failure, inviting a blind resubmit | Lost submission response | UI offers a "retry" | UNKNOWN is never failure and never auto-resubmits | `ActivityPage` state table + explicit no-retry copy; `e2e` asserts no Retry affordance; `requireFreshSubmissionAllowed` is protocol-owned | PASS | CRITICAL | — | None |
| PC-33 | web / a11y | Status is colour-only on a financial review screen | Colour-blind user | Distinguish FAILED from CONFIRMED by hue | Every status carries a text label and a glyph | `security.test.cjs` "critical status is not colour-only"; focus-visible ring, skip link, `sr-only` labels (re-executed in e2e) | PASS | MEDIUM | — | Contrast was not machine-audited for every token pairing |
| PC-34 | web / responsive | Horizontal overflow, hidden min_output, unreachable warnings at 360 px | Small phone | Resize to 360/390/430/768/1024/1440 | No overflow; critical fields remain visible | e2e overflow assertions at 390/1440/smallest width (re-executed, 96/96) | PASS | MEDIUM | — | None |
| PC-35 | web / outage UX | Outage is invisible; every table is empty; the user assumes the market has no pools | Ootle down (current reality) | Load app → tables empty → user infers "0 pools" | A global, honest outage surface | **NEW this pass:** `OutageNotice` in `AppShell`, driven by discovery health + `unavailableReason`, with render regressions in `test/render.test.cjs` ("the shell states the outage globally", "no banner while discovery is running", "a healthy source never renders the banner"), standing on the bounded transport of PC-01..PC-05 | FIXED | MEDIUM | Global banner plus honest per-page unavailable states | Per-page empty states remain for the non-outage case |
| PC-36 | web / console hygiene | Secrets or internals leak through `console` | Any code path | Log a secret or a rejection | The app never logs | `e2e/security.spec.ts` "the app logs nothing to the console" (re-executed) | PASS | HIGH | — | A `console.error` from third-party code would fail this test loudly, by design |

---

## F. External and tooling blockers (not closable from this environment)

| ID | Subsystem | Attack / gap | Prerequisite | State | Evidence | Result | Severity | Closure |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-37 | deployment | Security headers never observed on a real HTTPS origin | Cloudflare credentials | Headers authored, generated into `dist/_headers`, served by the local test server, asserted by CI and by browser tests — never seen on a deployed URL | `docs/TESTNET_HOSTING.md` § Exact minimal closure procedure | BLOCKED_EXTERNAL | HIGH (open) | The 5-step procedure in that document |
| PC-38 | wallet | Connector injection mechanism at `universe.tari.mw` not fully determined | Upstream wallet source | Live fetch this session returned a 932-byte `Tari Universe — L1 Wallet` shell that loads a WASM engine; `window.tari` and a `postMessage` bridge were **not** present in the outer document, so the injection point is still unconfirmed. Repository evidence shows the dApp runs cross-origin inside the wallet, so `frame-ancestors 'self' https://universe.tari.mw'` must stay until it is traced | EXTERNAL_RISK | HIGH (open) | Trace the wallet's dApp integration before tightening `frame-ancestors`; never tighten to `'none'`/`'self'` on assumption |
| PC-39 | network | Ootle testnet unavailable | DNS failure | `indexer.esmeralda.tari.com` and `indexer-fallback.tari.com` both failed name resolution this session; `esmeralda.tari.com` resolves but did not answer HTTPS. No pool discovery, market data, NFT discovery, wallet interaction, L2 swap, LP, or cross-layer flow could be executed — none is claimed | BLOCKED_EXTERNAL | HIGH (open) | Outage behaviour is now actively tested (PC-01..PC-05, PC-35) |
| PC-40 | tooling | Engine suite cannot run on Windows | `wasmer-compiler-cranelift` emits `compile_error` on Windows | Not executed locally and **not** claimed green | BLOCKED_TOOLING | INFO | Linux CI job `security-engine-tests.yml` |
| PC-41 | tooling | Firefox cannot run locally (software WebRender OOM instability) | Local renderer | Chromium desktop + mobile executed here; Firefox was not | BLOCKED_TOOLING | INFO | `node-tests.yml` `browser-security` job installs Firefox |
| PC-42 | wallet capability | Browser SHA swap path unavailable in the normal wallet path | Upstream capability | The UI shows a **blocker**, not a working button, and does not fall back to a local wallet service | `e2e/security.spec.ts` → "the browser SHA swap blocker is shown, not a working button" (re-executed) | EXTERNAL_RISK | MEDIUM | Upstream Tari wallet capability; architecture must not be weakened to paper over it |

---

## Totals

| Result | Count | IDs |
| --- | --- | --- |
| `PASS` | 34 | PC-11, PC-12, PC-13..PC-19, PC-20..PC-27, PC-28..PC-34, PC-36, PC-47, PC-48, PC-50, PC-52, PC-53, PC-55, PC-56, PC-60..PC-62 |
| `FIXED` | 16 | PC-01..PC-08, PC-35, PC-43..PC-46, PC-57..PC-59 |
| `N/A_BY_CONSTRUCTION` | 1 | PC-49 |
| `EXTERNAL_RISK` | 4 | PC-38, PC-42, PC-51, PC-54 |
| `BLOCKED_EXTERNAL` | 3 | PC-09, PC-37, PC-39 |
| `BLOCKED_TOOLING` | 3 | PC-10, PC-40, PC-41 |

---

## G. Second-pass protocol findings (independent model, client/chain parity)

These rows come from re-deriving the protocol semantics from the on-chain
templates rather than from the TypeScript, and from an oracle that does not call
the implementation.

| ID | Subsystem | Attack | Prerequisite | Exploit sequence | Invariant | Evidence | Result | Severity | Fix | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-43 | AMM | Quote a pool with a fee tier the chain cannot have created | Hostile/malformed authoritative readback | Report `fee_bps = 5000` → client accepted it (it allowed 1..9999) → a quote priced against an impossible pool | The client must not price state the chain refuses to create (`MAX_FEE_BPS = 1000`, `templates/fungible_pool/src/lib.rs`) | `packages/protocol-client/test/amm_parity.test.cjs` → "the client refuses fee tiers the chain can never have created"; "the highest fee tier the chain permits is still quotable" | FIXED | MEDIUM | `MAX_FEE_BPS` exported and enforced in `quoteSwapOutput` | Market-data's *observational* validator still accepts fee < 10000; harmless because discovery is non-authoritative |
| PC-44 | AMM | A/A "swap" of a resource into itself | Malformed pool (impossible on-chain) | Pool reports `resourceA === resourceB` → the direction check passes → the user reviews and signs a trade that can only abort | A pool's legs are always distinct (`check_pool_resources` asserts `a != b`) | `amm_parity.test.cjs` → "a swap of a resource into itself is refused before anything is built"; "a pool whose two legs are the same resource is refused" | FIXED | MEDIUM | Explicit same-resource refusal in `resolveSwap`, typed `CONFLICTED` | The documented claim now matches the code |
| PC-45 | AMM | Impossible pool state throws out of an async resolver | Malformed readback | One reserve is 0 → `resolveAddLiquidity` divides by zero outside any `try/catch`; `reserve_a: "abc"` reaches `BigInt()` | Resolvers return typed outcomes; they never throw | `amm_parity.test.cjs` → "a half-initialised pool is refused instead of dividing by zero"; "a malformed reserve string is rejected at the readback boundary"; "a hostile readback that returns garbage numbers yields UNAVAILABLE, not a throw" | FIXED | MEDIUM | `numericField` validation in `parsePoolState` (the readback boundary) **and** a `malformedNumericField` guard in each of the three resolvers, so the invariant does not depend on which readback produced the state; typed refusals for half-initialised pools and zero LP supply | The first parity run caught the resolver layer still throwing when a readback bypassed the parser, which is why the guard exists in both places |
| PC-46 | LP | Burn more LP than exists and display an impossible redemption | Caller-supplied `rawLpAmount` | `lp > totalLpSupply` → proportional amounts exceed the reserves → shown as a valid redemption | The displayed redemption must be payable by the pool | `amm_parity.test.cjs` → "burning more LP than exists is refused rather than quoted"; "burning the entire supply is still allowed" | FIXED | LOW | `lp > total` refused in `resolveRemoveLiquidity` | Unreachable in practice (LP is minted only by the pool) |
| PC-47 | AMM | Value extraction / rounding theft | Any trader | Compare the client quote against an independently derived k-invariant oracle over 4 000 randomised cases | `out == rOut - ceil(k/(rIn+e))` exactly, `k' >= k`, `out < rOut` | `packages/protocol-client/test/amm_model.test.cjs` → "the implementation matches the independent k-invariant oracle"; "a swap never reduces the constant product and never drains the reserve" | PASS | CRITICAL | — | Verified for the TypeScript mirror; on-chain execution evidence is CI-only (PC-10) |
| PC-48 | LP accounting | Rounding creates value across a deposit → redeem round trip | Any LP | Deposit, redeem the minted hint, compare with the deposit | A round trip never returns more than went in; a mint never exceeds the weaker proportional side | `amm_model.test.cjs` → "an add-liquidity hint never exceeds the weaker proportional side"; "a full round trip never returns more than was deposited" | PASS | CRITICAL | — | Asserted on a stable pool; real fees accrue between legs |
| PC-49 | LP | First-depositor / donation inflation | Any actor | Deposit a tiny first position, then donate value to capture the pool | First liquidity is floored and permanently locked | `templates/fungible_pool/src/lib.rs` `MINIMUM_INITIAL_LIQUIDITY` + `MINIMUM_LOCKED_LIQUIDITY`; reserves live in component-private vaults, so value cannot enter without minting shares | N/A_BY_CONSTRUCTION | CRITICAL | — | Only if a future change exposes a vault deposit or mints LP outside `add_liquidity` |
| PC-50 | NFT | Collection-bid escrow/quantity desync under partial fills, cancel, or expiry | Any seller filling a bid | Fill, cancel and expire in sequence, comparing escrow to quantity | `remaining_escrow == price_per_nft * remaining_quantity` at every step; a terminal state blocks further fills | `templates/nft_collection_bid/src/lib.rs` (`fill` checked_sub, escrow>=price guard, exactly-one-NFT guard, `effective_status` blocks post-expiry fills, one-time `refund_expired`) + engine suite in CI | PASS | CRITICAL | — | `remaining_quantity` stays non-zero on `Cancelled`/`Expired`; readers must honour the terminal `status` (the on-chain `bid()` reports `effective_status()`) |
| PC-51 | NFT | Escrow stranded by key rotation | Buyer loses/rotates the key that created the bid | `cancel` is gated on the *signing public key* captured at creation (`rule!(public_key(buyer_signer))`, `OwnerRule::None`); with `expires_at_epoch == 0` there is no other exit | Escrowed funds stay recoverable by the buyer | Identical access rules in all three NFT templates (uniform, deliberate design) | EXTERNAL_RISK | LOW | Documented in `security/PIXEL_CANARY_ARCHITECTURE_REVIEW.md` §8 | Funds are not stolen and not taken by a third party; the fix is a wallet-side authority decision, so no client change was made |
| PC-52 | Minotari script | Non-canonical varint makes our decoder disagree with the engine about the refund height | Malformed script | A 10-byte varint whose upper bits the engine silently drops | Client and engine must agree, or refuse | `decodeU64Varint` documents the engine's `shift > 63` truncation and **refuses** any encoding whose true value exceeds u64 | PASS | HIGH | — | Encodings the engine accepts but we cannot model are refused — the fail-closed direction |
| PC-53 | Minotari script | Extra opcodes, reordered branches, trailing bytes, truncated payloads | Malformed script | Mutate the funded script | Only the exact traced opcode sequence decodes | `decodeShaHtlcScript` asserts every opcode and both payload lengths and rejects trailing bytes; 50k structure-aware mutations in `hostile_fuzz.test.cjs` (re-executed) | PASS | HIGH | — | — |
| PC-54 | Minotari script | A preimage that hashes correctly but is not a canonical Ristretto point | Peer-generated S (TARI_TO_XTM) or the development secret store | The engine refuses the claim while our local `executeShaHtlcBranch` reports `ok` | Local verification must never be more permissive than the engine | `chains/minotari.ts` — the check is a *pre-flight* before the wallet RPC; the authoritative outcome is the chain's | EXTERNAL_RISK | LOW | Documented at `InMemorySecretStore.generateAndStore`; the production path is `ingestExternalSecret` (the wallet generates S) | A non-canonical peer preimage causes a failed claim, not a wrong settlement; canonicity detection needs curve math this client deliberately omits |
| PC-55 | Packaging | A duplicated module instance makes a nominal brand compare unequal | Two copies of `protocol-client` in one runtime | Import the package twice (e.g. CJS build + ESM build) | A nominal brand must be instance-local but consistent within an instance | The settlement-proof brand and `DisplayOnly` are both module-level `Symbol`s (runtime, not type-only); `packaging.test.cjs` verifies the CJS/ESM export shapes | PASS | HIGH | — | A cross-instance comparison fails closed (rejects), which is the safe direction |
| PC-56 | Frontend / perf | Market-data bundles accumulate for every pool ever visited; `follow()`/`stop()` are dead | A long session across many pools | Visit N pools → N `InMemoryMarketDataStore`s retained, never evicted | Bounded memory | `services/marketData.ts` (`bundles` map with no eviction; `follow`/`stop` uncalled) | PASS | LOW | Recorded in the architecture review §6/§8 | Per-bundle data is bounded (backfill limit, no live follow), so this is not an unbounded leak today |
| PC-57 | Test harness | A new regression file is silently inert because test discovery is an explicit file list | A contributor adds a test file | The file is written, looks like coverage, and never runs | Every committed regression must actually execute | The `test` script in `packages/protocol-client/package.json` was updated in this pass for both new suites; the reported test count is the check | FIXED | LOW | Script updated; risk recorded in the architecture review §3 | The structural footgun remains until discovery moves to a directory form |

---

## H. Third-pass findings (request liveness, and display-layer manipulation)

| ID | Subsystem | Attack | Prerequisite | Exploit sequence | Invariant | Evidence | Result | Severity | Fix | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-58 | wallet provider | A provider that never answers permanently disables the connect control | Hostile or broken injected provider | `request()` returns a promise that never settles → `connect()` awaits forever → status stays `CONNECTING` → the button stays disabled with no error and no way back | A broken provider must produce a recoverable error, not a dead control — and a signature awaiting a human must never be aborted | `apps/web/test/hostile-frontend.test.cjs` → "a non-interactive read from a silent provider times out", "a signing request is never given a deadline", "a read that answers in time is unaffected" | FIXED | MEDIUM | `PROVIDER_READ_TIMEOUT_MS` (15 s) applied in `tariWindow.call` to non-interactive methods only; `INTERACTIVE_METHODS` (sign + transaction-request methods) are deliberately unbounded so no artificial `UNKNOWN` submission can be manufactured | The existing e2e "a provider hanging forever cannot wedge the page" asserted only that the page renders; the control-level behaviour is now covered by a node regression |
| PC-59 | market data | A hostile indexer publishes a physically impossible price to paint a chart and rank pool volume | Control of the discovery source | Report a trade whose reserve movement implies a better-than-constant-product price → accepted into the candle series and the 24h volume ranking | A swap can never reduce the pool constant product: `k_after = (rA+in)(rB-out) >= k_before` | `packages/protocol-client/src/marketdata/store.ts` `validateObservation` (exact integer comparison) + `test/marketdata_hostile.test.cjs` → "k-reducing trade", "k-reducing trade (reverse direction)" | FIXED | MEDIUM | Constant-product invariant enforced before an observation becomes canonical | Deliberately does not assert "input reserve grew by exactly the input", because this repository does not fix whether the brackets cover the instruction or the whole transaction; asserting it could drop real trades |

---

## I. Cross-layer state machine, re-derived from the transition table

The rows below were read out of `crosschain/session.ts` and attacked state by
state, rather than taken from `security/CROSS_LAYER_ATTACK_MATRIX.md`.

| ID | Subsystem | Attack | Prerequisite | Exploit sequence | Invariant | Evidence | Result | Severity | Fix | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PC-60 | cross-layer | Reach secret disclosure without a verified, armed claim | A caller that fabricates verification events | Fabricate `L1_VERIFIED_FUNDED` / `L2_VERIFIED_FUNDED` to reach `BOTH_FUNDED`, then `ARM_CLAIM`, then `REVEAL_SECRET` | `REVEAL_SECRET` is reachable only from `CLAIM_ARMED`, and both verification handlers *throw* on any gap in the evidence | `session.ts` `TRANSITIONS` plus the `gaps` lists in both verification handlers; `hostile_crosschain.test.cjs` arming-without-proof cases (re-executed) | PASS | CRITICAL | — | A third-party adapter can still lie about the evidence it supplies (documented trust assumption) |
| PC-61 | cross-layer | Treat a lost submission response as failure and resubmit | A claim or refund whose response never arrives | `CLAIM_UNKNOWN` → `FAILED` → resubmit | UNKNOWN is never failure; it forces reconciliation | `TRANSITIONS`: `CLAIM_UNKNOWN` and `REFUND_UNKNOWN` both lead to `RECOVERY_REQUIRED`, and no terminal state has an outgoing edge | PASS | CRITICAL | — | Recovery is user- and wallet-driven |
| PC-62 | cross-layer | Re-drive a session after a crash, or let recovery silently resume the happy path | A crash at any point, or a hostile recovery record | Restart into a non-terminal state and re-run irreversible steps, or resolve recovery into `CLAIMING` | Terminal states are terminal; recovery is absorbing and never auto-resumes execution | `TRANSITIONS`: `CLAIMED` / `REFUNDED` / `FAILED_TERMINAL` have no outgoing edges and `RECOVERY_REQUIRED` only self-loops through `RECOVERY_RESOLVED`; hostile fuzz "a crash after every step never lets recovery re-drive a terminal session or blind-resubmit" (re-executed) | PASS | CRITICAL | — | A user must explicitly continue; that is the intended friction |





| **Total rows** | **62** | PC-01..PC-62, sequential, no gaps |
| `UNKNOWN` | **0** | — |

Open **CRITICAL**: **0**. Open **HIGH** that are code-fixable: **0**. Open HIGH
that are external: `PC-37` / `PC-09` (R-1, blocked on deployment credentials),
`PC-38` (wallet connector mechanism, blocked on upstream source), and `PC-39`
(Ootle unreachable).





