# LP HISTORICAL ATTACK MATRIX — fungible liquidity pool

Status vocabulary: `PASS` (executable evidence of resistance), `FIXED` (vulnerability found and
fixed; reproducer retained), `N/A_BY_CONSTRUCTION` (the Ootle execution model makes the class
impossible; evidence given), `EXTERNAL_RISK` (issuer/consensus/UI-level; router/UI mitigation
specified), `BLOCKED` (cannot be exercised with current tooling; exact missing capability stated).

Evidence keys:
- `ref:differential` = crates/pool_ref_model/tests/differential.rs (independent 256-bit model vs pool_math)
- `ref:gauntlet` = crates/pool_ref_model/tests/security_gauntlet.rs
- `ref:fuzz` = crates/pool_ref_model/tests/state_fuzzer.rs (100,000 biased operations, seeds 0x0F00_0000+i)
- `eng:e0x` = audit_engine_tests/tests/pool_engine.rs
- `eng:w0x` = audit_engine_tests/tests/pool_adversarial.rs
- `eng:E0x` = audit_engine_tests/tests/pool_economics.rs
- `eng:R0x` = audit_engine_tests/tests/opus08_eligibility.rs / opus08_recall_demo.rs

| ID | Historical class / example | Original assumption violated | Corresponding code path | Ootle applicability | Test / reproducer | Result | Severity | Mitigation | Residual risk | Evidence |
|----|---------------------------|------------------------------|------------------------|--------------------|-------------------|--------|----------|------------|---------------|----------|
| ATK-01 | Uranium Finance invariant/fee denominator mismatch (2022) | Fee denominator differed between execution and invariant validation; invariant checked against a different scale | `Pool::swap` fee math (`FEE_DENOMINATOR_BPS = 10_000`) is the single scaling used for `effective_input`, output, and k-growth; template, pool_math, protocol_types and UI all use bps/10_000 (OPUS-14 fix) | Applicable in principle; one coherent constant set | Tiny-input demanding disproportionate output aborts; exact output pinned (90_661_089 @ 1e9/1e9, 1e8 in); 2-unit input can never take ≥2 | PASS | CRITICAL if present | Single denominator; on-chain min_output | None under tested model | ref:fee_table, eng:e03/e03b, eng:w01 |
| ATK-02 | Uniswap V1 / ERC777 reentrancy | Untrusted callback during state transition | `swap`/`add_liquidity`/`remove_liquidity` make NO external component calls; only engine-native Vault deposit/withdraw; Ootle buckets are inert (no transfer hooks) | Callbacks do not exist for fungible resources; no hook surface | Code-path inspection + fuzz of interleaved op sequences (add/swap/remove in one world, atomicity asserted) | N/A_BY_CONSTRUCTION | CRITICAL if present | — | A future Ootle resource type with callbacks would require re-review | ref:fuzz (interleaved sequences, atomicity on every failed op) |
| ATK-03 | Balancer STA deflationary/reserve-accounting desync | Pool believed it received X but received less (transfer-fee token) | Ootle `Bucket.amount()` is engine-native: a bucket's amount is exactly what the vault receives; no on-transfer deduction exists for public fungibles | Transfers are engine-native; deflationary transfers impossible | Engine semantics: bucket deposited in full (`assert_eq!(reserve_a, ra + amt)` in every engine/ref test) | N/A_BY_CONSTRUCTION | HIGH if present | — | A future engine fee-on-transfer feature would require re-review | ref:fuzz reserve accounting; eng:w11 |
| ATK-04 | Curve mutable reentrancy (2021) | Getter used during state change returned stale/inconsistent data | Pool methods are self-contained; no cross-template calls; Ootle substate locking serializes mutating calls to the same component | Same-component calls serialize; no interleaving within one call | Sequential-conflict engine test (near-full swap twice, second strictly worse) | N/A_BY_CONSTRUCTION | HIGH if present | — | Cross-COMPONENT read skew is covered under ATK-22 | eng:w11 |
| ATK-05 | Balancer/Curve read-only reentrancy (2022) | View function observed inconsistent intermediate state during a nested call | Read methods (`get_pool_balances`) are separate transactions; Ootle executes one transaction's component calls under substate locks; reads cannot interleave mid-write | No nested execution path exists between pool calls | Sequential tooling model + engine execution semantics | N/A_BY_CONSTRUCTION | MEDIUM if present | UI must treat indexer reads as discovery only | Cross-component multi-tx reads can be sequentially inconsistent — by design | eng:w11; docs/SECURITY_MODEL.md |
| ATK-06 | Harvest flash-price manipulation | Spot price used as security input | Spot price / reserve ratio / swap quote / LP valuation have NO consumer: no collateral, minting, rewards, liquidation, governance, or stablecoin issuance reads them in this repo | Applicable only if a consumer is added | Repo-wide search (see report §Oracle) | N/A_BY_CONSTRUCTION (no consumer) | HIGH if consumer added | Never add spot-price-gated logic without TWAP + delay | Future consumers must re-open this row | repo search (grep reserve/quote/price consumers) |
| ATK-07 | Warp Finance oracle manipulation | LP share price used as collateral value | `lp_total_supply` exists but nothing derives value from it | No consumer | Repo search | N/A_BY_CONSTRUCTION | HIGH if consumer added | — | Same as ATK-06 | repo search |
| ATK-08 | PancakeBunny flash manipulation | Profit minted from manipulated spot | No profit/reward minting exists; only LP reserves grow from fees | No consumer | Repo search + fee/k-growth engine tests | N/A_BY_CONSTRUCTION | HIGH if present | — | — | eng:e03 |
| ATK-09 | ERC4626 first-depositor/donation inflation | First depositor mints huge share supply on tiny deposit; later victims donate via rounding | First deposit mints `floor(sqrt(a*b))` with `MINIMUM_INITIAL_LIQUIDITY = 1_000_000` on BOTH sides and 1_000 permanently locked LP; subsequent mints are proportional to PRE-deposit reserves; no donation path (both buckets required nonzero; vaults are component-private) | Applicable; defended | Attacker seeds 1e6/1e6, victim deposits 1e9/1e9: attacker redeems LESS than deposited; victim receives exactly proportional LP and exact redemption | PASS | HIGH | Geometric mean + locked minimum + pre-reserve proportionality | None under tested model | ref:first_depositor, eng:w04, eng:E0x (pool_economics) |
| ATK-10 | KyberSwap Elastic rounding/state mismatch (2023) | Rounding direction mismatch between state update and payout | Every division is floor, multiply-before-divide in 192-bit (`mul_div`), fee floors against the trader, redemptions floor toward the pool | Applicable; proven safe | Rounding gauntlet: reserves 1/1…3/2, near-empty, huge/tiny ratios, 10k repeated same-direction tiny swaps, alternating swaps, split vs single | PASS | CRITICAL | All-floor arithmetic; wide intermediates | None under tested model | ref:gauntlet, eng:w03 |
| ATK-11 | Repeated precision extraction (iterative) | Repeated tiny swaps compound rounding in attacker's favor | Output per direction is monotone non-increasing; fragmented trades ≤ aggregate; round-trips only lose | Applicable; proven safe | 10,000-iteration same-direction loops; 5,000-iteration alternations; 2,000×1,000 randomized round-trip loops; 6-round engine micro-round-trips | PASS | CRITICAL | Fee floor + output floor | None under tested model | ref:gauntlet, eng:w03 |
| ATK-12 | Balancer-style precision/rounding drain classes | Per-vault rounding inconsistencies | Single shared `mul_div`; both legs of remove_liquidity floor toward pool; LP mint uses min() of both sides | Applicable; proven safe | Differential 20,000 randomized + boundary matrix vs independent 256-bit model; fuzz asserts proportional mint/redeem on every op | PASS | CRITICAL | — | None under tested model | ref:differential, ref:fuzz |
| ATK-13 | Zero-supply / reinitialization LP attacks | Pool re-enters first-deposit path and over-mints shares | After full exit, reserves remain nonzero dust (1000/1000) backed by locked LP, so the both-reserves-zero branch is unreachable; re-entry mints proportional shares | Applicable; proven safe | Empty pool to locked-only, re-deposit, redeem; locked stays exactly 1_000 (re-bootstrapping would re-lock) | PASS | HIGH | Locked LP anchors reserves | None under tested model | ref:zero_user_lp_reinit, eng:w05 |
| ATK-14 | LP over-mint / under-burn | Mint/redeem ratios favor the caller | `min()` of both proportional sides on mint; floor on both redemption legs; zero-mint and zero-redemption abort atomically | Applicable; proven safe | Fuzz asserts exact `floor(amount*supply/reserve)` mint and `floor(lp*reserve/supply)` redeem on every op; near-max reserves covered | PASS | CRITICAL | — | None under tested model | ref:fuzz, ref:arithmetic_near_amount_max |
| ATK-15 | Malicious underlying asset (deflationary/rebasing/owned) | Token behaves adversarially after pooling | Pool accepts canonical Tari + ordinary public fungibles by authoritative type; recallable/freezable/mutable-rule fungibles CANNOT be screened by the v0.41.1 template ABI (no access-rule introspection) | Issuer-recall drain is REAL and demonstrated (pool holdings of a recallable token can be stolen by the issuer) | Recall drain demo; eligibility matrix (stealth/NFT/confidential/fake-Tari rejected) | EXTERNAL_RISK | HIGH | Router/UI MUST warn on issuer-authority tokens (advisory `unsafe_recallable`/`unsafe_freezable` verdicts from indexer facts); do not weaken OPUS-08 type policy | Issuer-level theft of the recallable side; fee/k invariants of the other side hold | eng:R04/R05/recall_demo; client classifyResource |
| ATK-16 | Sandwich / front-run / back-run | Victim gets worse price than quoted | On-chain `min_output` enforced in `swap`; aborts atomically; no other protection exists (permissionless mempool) | Applicable; bounded | min_output abort + atomicity engine tests | PASS (bug class) / EXTERNAL_RISK (economic MEV) | MEDIUM (economic) | Users must set min_output from a trusted quote at sign time; wallet should set expiry | Sandwiching remains economically possible; it is market behavior, not a drain | eng:e04, eng:w09, eng:w01 |
| ATK-17 | Just-in-time liquidity | LP adds around a large swap to capture fees | Fee accrues to ALL LPs pro-rata; JIT captures a share — economic behavior, not accounting bug | Applicable | Model: LP share of fees is exactly proportional (`lp*reserve/total`); no accounting asymmetry found | PASS (accounting) / economic behavior | LOW (economic) | Optional add/remove lockup if desired later (not added) | JIT fees are a design tradeoff, quantified pro-rata | ref:fuzz redemption proportionality |
| ATK-18 | Stale quote/state races | Client signs on stale state | Chain state is authoritative; on-chain min_output is the bound; transaction expiry/epoch enforced by the engine layer | Applicable; bounded | Client policy documented (indexer = discovery); w11 sequential staleness | EXTERNAL_RISK (UI) | MEDIUM | Router must re-quote at sign time; set expiry/max epoch in intents | A stale quote without min_output can be sandwiched — user-configured bound required | eng:w11; docs/SECURITY_MODEL.md |
| ATK-19 | Replay | Same withdrawal executes twice | Ootle transactions are sealed/hashed and include inputs by reference; a re-submitted identical transaction operates on CURRENT (post-first-execution) state, so a second withdrawal finds no funds; failed transactions commit nothing | Applicable; engine-level | w11 (second identical near-full swap strictly worse / cannot repeat withdrawal) | N/A_BY_CONSTRUCTION (engine tx layer) + PASS | HIGH | — | None under tested model | eng:w11, eng:w09 (failed tx commits nothing) |
| ATK-20 | Overflow / underflow | Silent wrapping in release WASM | All products via `into_precision_amount()` (192-bit) `checked_*`; `mul_div` panics rather than wraps; template release profile disables overflow checks → wide intermediates mandatory | Applicable; proven safe | Near-u128::MAX reserves/supplies in the 256-bit model; engine aborts atomically on genuine range overflow (Amount try_from) | PASS | CRITICAL | 192-bit intermediates; checked ops | Amount-range exhaustion aborts (DoS, not drain) | ref:arithmetic_near_amount_max, ref:differential |
| ATK-21 | DoS / dust-state attacks | Pool bricked into unusable state | Dust reserves still function (floor semantics); zero-effective-input swaps abort cleanly; failed ops atomic; massive deposits capped only by supply; dust LP mints abort | Applicable; partially inherent | Gauntlet dust scenarios (1/1 reserves); engine 1-unit abort; reinit-from-dust works | PASS | LOW | — | A maximal-fee pool (1000 bps) floors most trades — constructor-bounded, visible on-chain | ref:gauntlet, eng:w02, eng:w05 |
| ATK-22 | Concurrency / stale state (substate locking) | Two conflicting withdrawals both commit | Ootle consensus applies substate locks; only one of two conflicting transactions can commit | Applicable at consensus layer | Test tooling is sequential — cannot construct true lock contention | BLOCKED | HIGH if absent | Engine/consensus guarantee relied upon; document | Exact missing capability: tari_template_test_tooling has no concurrent-transaction API; verify on live testnet before mainnet | docs/ESMERALDA_VERSION_TARGET.md |
| ATK-23 | LP token forgery / fake resource with same symbol | Identity by metadata | Identity is exact `ResourceAddress` everywhere (pair canonicalization, LP bucket check, swap output); symbol "TARI" look-alike is an ordinary fungible | Applicable; proven safe | Fake-Tari engine tests; canonical_pair ordering by address Ord | PASS | CRITICAL | Address-based identity | None under tested model | eng:R03/R10, eng:w08 |
| ATK-24 | Unauthorized LP mint / burn / privileged drain | Creator/owner mints or drains | LP mint AND burn rules are `component(this_pool)` with `OwnerRule::None`, rules Locked; no admin methods exist; default access rule DenyAll | Applicable; proven safe | Untrusted-component mint attempt rejected; untrusted-component burn rejected; nonexistent admin methods all fail | PASS | CRITICAL | — | None under tested model | eng:w06, eng:w07, eng:w10, eng:e01 |
| ATK-25 | Donation / forced-balance manipulation | Attacker increases reserves without minting | Reserve vaults are component-private; the ONLY entry points are add_liquidity (requires both buckets, mints proportional LP) and swap (full input against exact output); no method accepts one-sided deposits | Applicable; no donation path | Code-path closure of all public methods (w10 enumerates nonexistence); fuzzer conservation holds | N/A_BY_CONSTRUCTION | HIGH | — | None under tested model | eng:w10, ref:fuzz conservation |
| ATK-26 | Fee-on-transfer analogue (deflationary bucket) | Vault receives less than bucket amount | Ootle fungible transfers are engine-native amounts; bucket amount == vault credit | Impossible for engine-native fungibles | Reserve accounting equality asserted in every fuzz op and engine test | N/A_BY_CONSTRUCTION | HIGH | — | Only if the engine ever introduces transfer fees | ref:fuzz, eng:e03 |

**Totals: 26 rows — PASS 13, N/A_BY_CONSTRUCTION 8, EXTERNAL_RISK 3, BLOCKED 1 (ATK-22, consensus-layer tooling), FIXED 1 (OPUS-14 fee denominator, historical, now pinned by e03/w01/fee_table), UNKNOWN 0.**
